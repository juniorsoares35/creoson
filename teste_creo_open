import creopyson

# Conectar ao Creo
c = creopyson.Client()
c.connect()
id = c.sessionId
creo_open = c.is_creo_running()

if creo_open == False:
    input(print("creo nao foi aberto corretamente"))
    input("Aperte enter para sair...")
    exit()
else:
    print("Creo aberto")